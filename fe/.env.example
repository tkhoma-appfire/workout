# Optional: point the dev frontend at a remote API instead of the Vite proxy.
# Leave unset for same-origin /api (recommended with npm run dev:fe + npm run dev:be).
# VITE_API_BASE_URL=http://localhost:8081
